package com.coursetable.app.ui

enum class RootDestination { TIMETABLE, COURSES, MORE }
enum class QuickAction { ADD_COURSE, EDU_IMPORT, FILE_IMPORT }
enum class ImportEntry { HUB, EDU, FILE, BACKUP, INCOMING }
